---
phase: 09-fate-of-the-fellowship
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified: []
autonomous: false
requirements: [FOTF-01, APP-02, APP-03, APP-04]

must_haves:
  truths:
    - "npm run build succeeds cleanly with no build-time localStorage/window error and no SSR adapter added"
    - "dist/armory/fate-of-the-fellowship.html carries a per-app <title>, meta description, and OG/Twitter tags (not the site defaults) plus a static crawlable intro outside the island"
    - "The app is playable (setup, step/objective tracking, hope/army trackers), progress persists across reload, and explicit reset works — with the Nocturne nav/footer/buttons on that page visually unaffected by the app's CSS"
    - "The armory route is included in the sitemap (crawlable) and the marketing pages gained no new script/style tags"
  artifacts:
    - dist/armory/fate-of-the-fellowship.html
  key_links:
    - "the sitemap filter excludes only /admin, so /armory/fate-of-the-fellowship is included automatically"
    - "the per-app <title>/<meta>/OG in the built <head> come from the armoryApps.ts registry entry via Layout props"
    - "the app's CSS is scoped under .ff, so Nocturne Nav/Footer on the same page render identically to other routes"
---

<objective>
Prove Phase 9's acceptance criteria against the actual production build and a live play-through: the automated SEO / CSS-scope / crawlability / no-regression gates, then a blocking human verification that the app plays correctly inside the Nocturne shell with persistence and reset.

Purpose: `npm run build` (not astro dev) is the hard acceptance gate for this milestone — it is the cheapest catch for build-time localStorage/window reads and CSS-leak regressions. This plan turns each success criterion into a checkable gate and a human play-through.
Output: Verified evidence that all five Phase 9 success criteria hold; a blocking checkpoint confirming playability and shell integrity.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-fate-of-the-fellowship/09-CONTEXT.md
@.planning/phases/09-fate-of-the-fellowship/09-01-SUMMARY.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated acceptance gates — build, SEO head, CSS scope, crawlability, no marketing-page regression</name>
  <files>dist/armory/fate-of-the-fellowship.html</files>
  <action>
  Run a clean production build and assert each success criterion against the emitted files (all commands from the project root C:/projects/darktier-studio). Do not modify source in this task — if a gate fails, record it precisely so it can be routed back to 09-01 as a gap.

  1. Build + static output (Success #5): `npm run build` exits 0; dist/armory/fate-of-the-fellowship.html exists. Also assert astro.config.mjs still has no `output`/`adapter` key (free-Spark/static constraint — PITFALLS 8): the site must stay fully static.

  2. Per-app SEO in the INITIAL HTML (Success #4 / APP-04): grep the built dist/armory/fate-of-the-fellowship.html for the per-app title text (from the registry entry, e.g. "Fate of the Fellowship") appearing in the <title> and in og:title/twitter:title, and for the app-specific description appearing in <meta name="description"> and og:description — and confirm these are NOT the generic site defaults (compare against dist/index.html's title/description, which should differ). The head is server-rendered by Layout even though the app body is client:only, so these must be present in the raw HTML.

  3. Static crawlable content outside the island (Success #5 / PITFALLS 2b): confirm the built HTML body contains the static intro text (the registry name and description rendered outside the island) — i.e. real crawlable content exists beyond the empty client:only mount node.

  4. CSS fully scoped, no leak (Success #2 / APP-02 / PITFALLS 3): confirm no unscoped selector leaked into the built CSS or the component. Re-run the source gate `test "$(grep -nE '^\.[A-Za-z]' src/components/armory/FateOfTheFellowship.tsx | grep -vcE ':\.ff')" = "0"`, and confirm the app's own generic class names do not appear as bare (un-prefixed) selectors in any built stylesheet under dist/ (they must only ever appear as descendants of .ff).

  5. Crawlability / sitemap (spec: armory route SHOULD be crawlable, NOT in the /admin exclusion): confirm dist/sitemap-0.xml (or the sitemap index it points to) contains /armory/fate-of-the-fellowship, and confirm the built armory HTML does NOT carry a `noindex` robots meta.

  6. No marketing-page regression (PITFALLS 5): confirm dist/index.html and dist/tools.html did not gain new <script>/<link> tags pointing at the armory island's JS/CSS chunk — the app JS must be code-split to the /armory route only, keeping public pages ~0KB JS.

  7. Build-time safety (Success #3): the successful build already proves the component module has no top-level localStorage/window access (the registry imports it server-side during build); note this explicitly.
  </action>
  <verify>
    <automated>cd C:/projects/darktier-studio && npm run build && test -f dist/armory/fate-of-the-fellowship.html && grep -qi 'fate of the fellowship' dist/armory/fate-of-the-fellowship.html && grep -Eq '<meta[^>]+og:title[^>]+[Ff]ate of the [Ff]ellowship' dist/armory/fate-of-the-fellowship.html && grep -q 'armory/fate-of-the-fellowship' dist/sitemap-0.xml && ! grep -qi 'noindex' dist/armory/fate-of-the-fellowship.html && test "$(grep -nE '^\.[A-Za-z]' src/components/armory/FateOfTheFellowship.tsx | grep -vcE ':\.ff')" = "0" && ! grep -qE '"(output|adapter)"|output:|adapter:' astro.config.mjs</automated>
  </verify>
  <done>All seven gates pass: clean static build with dist/armory/fate-of-the-fellowship.html present and in the sitemap (not noindexed); per-app title/description/OG in the initial HTML differ from site defaults; static intro text present outside the island; zero unscoped selectors in the component and no bare app selectors in built CSS; Home/Tools gained no new script/link tags; no SSR adapter added.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human play-through — playable, shell intact, persistence + reset</name>
  <action>Blocking human verification (do not auto-approve). Preview the production build and have the owner confirm the five checks in how-to-verify, then wait for the resume signal before proceeding.</action>
  <what-built>Fate of the Fellowship is served at /armory/fate-of-the-fellowship as a client:only React island inside the Nocturne nav/footer shell, with its CSS scoped under .ff, persistence on real localStorage (key fotf:v2), and a static crawlable intro above the app.</what-built>
  <how-to-verify>
  1. Preview the production build: run `npm run preview` and open http://localhost:4321/armory/fate-of-the-fellowship (use the port the preview prints).
  2. Playability (Success #1 / FOTF-01): complete game setup (players, difficulty), then exercise step/objective tracking and the hope and army trackers — all should behave as they did standalone.
  3. Nocturne shell + no CSS leak (Success #2 / APP-02): confirm the site Nav (top) and Footer render normally on this page and look identical to how they render on /tools and / — the Nocturne buttons/nav must NOT be restyled by the app. The app keeps its own dark look inside its region; the app's own fixed bottom tab bar is part of that look and is expected. Open a second tab on / and /tools to compare the nav/footer/buttons.
  4. Persistence + reset, no login (Success #3 / APP-03): change some trackers, reload the page — your progress should restore. Then use the app's reset/new-game control and confirm it returns to defaults and that reload after reset shows the reset state. No login should be required at any point.
  5. Share preview (Success #4 / APP-04): view page source and confirm the <title>, description, and OG/Twitter tags are specific to Fate of the Fellowship, not the generic site defaults.
  </how-to-verify>
  <resume-signal>Type "approved" if all five hold, or describe what failed (which criterion, what you saw) so it can be routed back to 09-01 as a gap.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nocturne shell ↔ app island | verification that the app's `<style>` block does not restyle Nav/Footer on the shared page |
| browser ↔ localStorage | verification that persistence/reset works only against the user's own `fotf:v2` key with no login/network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-02 | Information disclosure | app CSS-in-JS leaking into Nocturne pages | low | mitigate | Verified here by the built-CSS scope grep (Task 1.4) and the human nav/footer comparison against / and /tools (checkpoint step 3) |
| T-09-05 | Spoofing | share-preview / SEO tags | low | mitigate | Verified the per-app title/description/OG in the initial HTML are the app's own, not spoofable site-default fallbacks (Task 1.2, checkpoint step 5) |
| T-09-SC | Tampering | package installs | low | accept | Not applicable — this plan installs nothing and modifies no source; verification only |
</threat_model>

<verification>
- `npm run build` succeeds; dist/armory/fate-of-the-fellowship.html exists, is in the sitemap, and is not noindexed.
- Per-app title/description/OG present in the built head and distinct from site defaults; static intro text present outside the island.
- No unscoped selector in the component; no bare app selector in built CSS; Home/Tools gained no new script/link tags; no SSR adapter.
- Human checkpoint: app playable, shell intact, persistence + reset work with no login, share tags app-specific.
</verification>

<success_criteria>
All five Phase 9 ROADMAP success criteria are demonstrated: (1) playable, (2) full-screen in the Nocturne shell with fully-scoped CSS and no Nocturne page affected, (3) localStorage persistence + explicit reset with no login and post-mount access only, (4) real per-app title/description/OG in the initial HTML, (5) `npm run build` emits dist/armory/fate-of-the-fellowship.html with static crawlable content outside the island.
</success_criteria>

<output>
Create `.planning/phases/09-fate-of-the-fellowship/09-02-SUMMARY.md` when done
</output>
