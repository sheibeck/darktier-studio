---
phase: 260818-l22
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/armory/FateOfTheFellowship.tsx
autonomous: false
requirements: [QUICK-260818-l22]
must_haves:
  truths:
    - The Fate of the Fellowship main tab menu (Setup/Turn/Dice/Rules) is a sticky bar at the TOP of the app that stays visible while scrolling, matching the Burning Banners companion.
    - The tab menu no longer sits fixed at the viewport bottom and no longer overlaps the Nocturne site footer.
    - The existing Hope/Threat/Shadow status bar is still visible and stacks BELOW the sticky nav — the two bars never overlap.
  artifacts:
    - src/components/armory/FateOfTheFellowship.tsx
  key_links:
    - "`.ff .nav` uses `position:sticky;top:0` (not `position:fixed`) so it pins to the top of the scroll flow."
    - "The `<nav className=\"nav\">` element is the FIRST child inside `.body`, ahead of `.status`, so the sticky top actually anchors at the top."
    - "`.ff .status` carries a `top:58px` offset (roughly the nav's own height) so it stacks under the nav rather than fighting it for `top:0`."
---

<objective>
Convert the Fate of the Fellowship companion's main tab menu from a bar fixed to the bottom of the viewport (which overlaps the Nocturne site footer) into a sticky bar pinned to the TOP of the app — mirroring the Burning Banners companion's sticky-top header.

Purpose: The bottom-fixed nav overlaps the shared site footer and behaves differently from the sibling companion (Burning Banners), which is jarring. A sticky-top nav sits inside normal scroll flow, clears the footer, and matches the established companion pattern.

Output: Edits confined to `src/components/armory/FateOfTheFellowship.tsx` — three CSS rule changes plus one JSX reorder. No new dependencies, no behavior change to the tabs, no restyle of anything else.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@src/components/armory/FateOfTheFellowship.tsx
@src/components/armory/BurningBanners.tsx

Grounding facts (verified against the file):
- CSS `.ff .nav` (~line 384) is `position:fixed;left:0;right:0;bottom:0;z-index:30;display:flex; background:rgba(11,14,20,.97);backdrop-filter:blur(10px);border-top:1px solid var(--line); …env(safe-area-inset-bottom)…}` — this fixed-to-viewport bar is what overlaps the footer.
- CSS `.ff .body` (~line 243) is `…max-width:640px;margin:0 auto;padding:0 16px 108px;` — the `108px` bottom padding exists ONLY to clear the fixed bottom nav.
- CSS `.ff .status` (~line 246) is an EXISTING sticky-top readout: `position:sticky;top:0;z-index:20;margin:0 -16px;padding:0 16px;background:rgba(11,14,20,.95);backdrop-filter:blur(10px);border-bottom:1px solid var(--line);}` — after the nav becomes sticky-top, this must STACK below the nav, not share `top:0`.
- Render (~line 949): `<div className="ff"> <style/> <div className="amb"/> <div className="body"> <div className="status">…</div> {tab views} </div> <nav className="nav">…</nav> </div>`. The `<nav className="nav">` currently sits AFTER `.body`.
- Nav height math: `.nb` buttons are `padding:9px 0 10px` (19px), icon svg 23px, `gap:3px`, label `font-size:11px` (~13px line box), plus the nav's own `border-bottom` 1px → ~59px total. So a `top:58px` offset on `.status` tucks it just under the nav (nav's `z-index:30` covers any 1px seam; a slightly-under value avoids a visible gap).
- Reference — Burning Banners sticky-top header (BurningBanners.tsx ~line 2551): `position: sticky; top: 0; background: rgba(11,17,29,0.94); borderBottom: 1px solid <line>; backdropFilter: blur(8px); zIndex: 20`. Its inner search bar stacks at `top:14` (56px) beneath the header — the same stacking idea used here.
- This island is `client:only`, so there is no SSR concern; `npm run build` is the gate.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip the nav to a sticky-top bar, reorder it in JSX, and stack the status bar below it</name>
  <files>src/components/armory/FateOfTheFellowship.tsx</files>
  <action>
Make four edits, all inside `src/components/armory/FateOfTheFellowship.tsx`. Keep the existing dark/translucent/blurred look — reuse the nav's own background, blur, and border tokens; only flip which edge it borders and where it pins.

1. Rewrite the `.ff .nav` CSS rule (currently the `/* ---- bottom nav ---- */` block near line 384). Change it from a viewport-pinned bar to a sticky-top bar: `position:sticky;top:0;z-index:30;display:flex; margin:0 -16px;padding:0 16px; background:rgba(11,14,20,.97);backdrop-filter:blur(10px);border-bottom:1px solid var(--line);`. Concretely: drop the `left`/`right`/`bottom` viewport pins, drop the `env(safe-area-inset-bottom)` bottom padding (it is only meaningful for a bottom bar), switch `border-top` to `border-bottom`, and give it `margin:0 -16px;padding:0 16px` so it spans the body's content edges flush — exactly the treatment the existing `.status` bar already uses. Keep `z-index:30` so the nav paints above the `z-index:20` status bar. Leave the `.nb`, `.nb.on`, and `.nb svg` rules untouched. Update the `/* ---- bottom nav ---- */` comment to `/* ---- top nav ---- */` so the label matches reality.

2. Change the `.ff .body` bottom padding (near line 243) from the `108px` value that reserved space for the old bottom bar down to a small normal gap: `padding:0 16px 24px;`. Keep the top padding at `0` (the sticky bars still need to sit flush at the top) and keep `max-width:640px;margin:0 auto`.

3. Change the `.ff .status` sticky offset (near line 246) so it stacks under the nav instead of fighting it for the top edge: change its `top` from the flush top edge to `top:58px` (roughly the nav's own height per the nav-height math in context). Leave everything else on `.status` unchanged (`position:sticky`, `z-index:20`, `margin:0 -16px`, `padding:0 16px`, background/blur/border-bottom all stay).

4. Reorder the JSX (render block near line 949) so the sticky nav actually anchors at the top of the scroll flow. Move the entire `<nav className="nav"> … </nav>` element from its current position (after the closing `</div>` of `.body`, before the closing `</div>` of `.ff`) to be the FIRST child INSIDE `<div className="body">`, immediately BEFORE `<div className="status">`. A `position:sticky` element only sticks within/after its own position in normal flow, so it must precede the content it should stay above. Delete the old trailing `<nav>` block entirely — there must be exactly one `<nav className="nav">` in the file, and it must render before `.status`. Do not change the nav's contents (the `NAV.map(...)` buttons, `ICONS`, `setTab`, `aria-current`) — this is a pure move.
  </action>
  <verify>
    <automated>F=src/components/armory/FateOfTheFellowship.tsx; grep -q 'position:sticky;top:0;z-index:30' "$F" && [ "$(grep -c 'padding-bottom:env' "$F")" -eq 0 ] && [ "$(grep -c '\.ff \.status{position:sticky;top:0' "$F")" -eq 0 ] && grep -q '\.ff \.status{position:sticky;top:58px' "$F" && [ "$(grep -c 'className="nav"' "$F")" -eq 1 ] && n=$(grep -n 'className="nav"' "$F" | head -1 | cut -d: -f1) && s=$(grep -n 'className="status"' "$F" | head -1 | cut -d: -f1) && [ "$n" -lt "$s" ] && npm run build</automated>
  </verify>
  <done>
`.ff .nav` is `position:sticky;top:0;z-index:30` with `border-bottom` and no `env(...)` safe-area padding; `.ff .body` bottom padding is `24px` (no `108px`); `.ff .status` is offset to `top:58px`; the single `<nav className="nav">` renders before `<div className="status">` inside `.body`; and `npm run build` succeeds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify the sticky-top nav, status stacking, and footer clearance</name>
  <action>Human visual verification only — no code changes. Run the dev server, open the companion, and confirm the nav is sticky at the top, the status bar stacks cleanly below it, and the site footer is no longer overlapped (steps below). If the two bars overlap or leave a gap, nudge the `.ff .status` `top` value and re-run `npm run build`.</action>
  <what-built>The Fate of the Fellowship main tab menu is now a sticky bar at the top of the app instead of fixed to the bottom. The Hope/Threat/Shadow status bar was offset to `top:58px` so it stacks directly beneath the nav.</what-built>
  <how-to-verify>
1. Run `npm run dev` and open the companion at `/armory/fate-of-the-fellowship`.
2. Confirm the tab menu (Setup / Turn / Dice / Rules) now sits at the TOP of the app, not the bottom.
3. Scroll a tab view (e.g. Rules) up and down — confirm the tab menu stays pinned to the top and stays visible the whole time (matching Burning Banners' sticky header).
4. Confirm the Hope/Threat/Shadow status bar sits directly BELOW the tab menu with no overlap and no visible gap/seam between them. Expand the status sheet (tap it) and re-scroll to confirm both bars still behave.
5. Scroll to the very bottom of a tab and confirm the Nocturne site footer is fully visible and NO LONGER overlapped by the tab menu.
6. If the status bar overlaps the nav or leaves a visible gap, adjust the `.ff .status` `top` value (nudge up/down from `58px`) until they stack cleanly, then re-run `npm run build`.
  </how-to-verify>
  <resume-signal>Type "approved" once the nav is sticky at the top, the status bar stacks cleanly below it, and the footer is no longer overlapped — or describe the overlap/gap to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Cosmetic CSS/JSX reorder in an existing `client:only` island. No new data flow, no user input, no network calls, no auth, no package installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260818-l22-01 | Tampering | FateOfTheFellowship.tsx render output | low | accept | Change is a positional/style-only edit to one existing island; no new inputs, dependencies, or trust boundaries. `npm run build` is the integrity gate. No new attack surface introduced. |
</threat_model>

<verification>
- `npm run build` passes (the release gate for this `client:only` island).
- Automated greps confirm: `.ff .nav` is `position:sticky;top:0;z-index:30`; the old `env(...)` safe-area bottom padding is gone; `.ff .status` no longer pins at the top edge and now carries a `top:58px` offset; exactly one `<nav className="nav">` exists and it renders before `.status`.
- Human-verify: nav sticks at the top while scrolling, status bar stacks cleanly beneath it, and the Nocturne footer is no longer overlapped.
</verification>

<success_criteria>
- The main tab menu is a sticky bar at the top of the Fate of the Fellowship companion, staying visible while scrolling (matching Burning Banners).
- The tab menu no longer overlaps the site footer at the bottom.
- The Hope/Threat/Shadow status bar remains visible and stacks below the nav without overlap.
- Only `src/components/armory/FateOfTheFellowship.tsx` changed; no new dependencies; tab behavior unchanged; `npm run build` succeeds.
</success_criteria>

<output>
Create `.planning/quick/260818-l22-make-fate-of-the-fellowship-main-menu-a-/260818-l22-SUMMARY.md` when done.
</output>
