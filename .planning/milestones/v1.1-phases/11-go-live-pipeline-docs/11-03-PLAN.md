---
phase: 11-go-live-pipeline-docs
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified: []
autonomous: false
requirements: [APP-01]

must_haves:
  truths:
    - "The owner has the deploy-ready state plus a single checklist covering every remaining go-live action."
    - "Deploy (SC5) and creating the LIVE production Firestore tool docs (SC4) are performed by the owner, not autonomously."
  artifacts: []
  key_links:
    - "checkpoint ↔ docs/go-live-v1.1.md (the compiled owner handoff)"
---

<objective>
Hand the verified, deploy-ready milestone to the owner for the outward-facing go-live steps that must not
run autonomously: the production `npm run deploy`, seeding the two LIVE production Firestore tool docs,
and the accumulated browser play-throughs.

Purpose: SC5 (deploy to darktierstudios.com) and the SC4 LIVE production Firestore docs are explicitly
OWNER-DEFERRED — outward-facing, owner-authorized actions. This plan isolates them as a single blocking
checkpoint (mirroring how Phases 9 and 10 isolated their human steps) so the autonomous work can be
marked complete while the owner performs go-live when ready. This is the live realization of APP-01
(both apps reachable in-site on the production domain).

Output: owner confirmation of go-live, or an explicit defer that closes the phase with SC5 outstanding.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-go-live-pipeline-docs/11-CONTEXT.md
@docs/go-live-v1.1.md
@LAUNCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Owner go-live handoff — deploy + live Firestore seed + play-throughs</name>
  <files>docs/go-live-v1.1.md</files>
  <action>
Present the deploy-ready state to the owner and hand off the remaining outward-facing go-live steps.
Do NOT run `npm run deploy` or write to production Firestore autonomously — these are owner-authorized
actions (SC5 and the live SC4 docs are OWNER-DEFERRED). Direct the owner to docs/go-live-v1.1.md and
wait at this blocking gate for their approval or explicit defer.
  </action>
  <what-built>
The code/seed side of go-live is done and verified autonomously:
- Fate of the Fellowship and Burning Banners are seeded in src/data/catalog/tools.ts as status:"live",
  kind:"internal", slugs matching the registry exactly (11-01).
- A zero-regression seed-fallback build confirmed the built /tools page lists both apps live with same-tab
  /armory links, both /armory routes exist and are in the sitemap, and the marketing pages carry no
  companion-app JS/CSS on a fully static (no output/adapter) free-Spark build (11-01).
- The drop-in pipeline guide, the CLAUDE.md scoped-Tailwind exception, and the compiled v1.1 go-live
  checklist are written (11-02).

The remaining steps are outward-facing OWNER actions — deploy and live production Firestore docs — which
are deliberately NOT run autonomously.
  </what-built>
  <how-to-verify>
Work through docs/go-live-v1.1.md, in order:

1. Seed the LIVE production Firestore `tools` collection with both records (SC4) — via the admin CMS
   "Load starter catalog" or the seed script — each slug matching the registry exactly (fate-of-the-fellowship,
   burning-banners), status:"live", kind:"internal".
2. Deploy: run `npm run deploy` (build + `firebase deploy --only hosting`) when ready (SC5).
3. On the LIVE site, open https://darktierstudios.com/tools — confirm both apps show as live tools and
   each "Launch" opens in-site at /armory/<slug> without leaving the domain (SC1 in production).
4. Play-throughs / spot-checks carried over this milestone:
   - Fate of the Fellowship at /armory/fate-of-the-fellowship — playable, Nocturne shell intact,
     progress persists across reload, reset works.
   - Burning Banners at /armory/burning-banners — basic/advanced modes, trackers, save + reset; and a
     glance at the FOTF armory page re: the shared-template CSS bundling note.
   - Admin: create/edit an internal-kind tool and confirm same-tab Launch behavior.
  </how-to-verify>
  <resume-signal>
Type "approved" once deployed and both apps are confirmed live on darktierstudios.com, OR type "defer"
to close the phase with the deploy/live-Firestore steps left as a standing owner checklist (SC5 and the
live SC4 docs remain outstanding by design).
  </resume-signal>
  <verify>
    <human-check>Owner confirms both apps are live on darktierstudios.com and each Launch opens in-site at /armory/<slug>, or explicitly defers with a standing checklist.</human-check>
  </verify>
  <done>
Owner has either approved go-live (deployed, both apps confirmed live on darktierstudios.com) or
explicitly deferred, leaving docs/go-live-v1.1.md as the standing owner checklist.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner → production (Firebase Hosting + Firestore) | Deploy and live-data writes are owner-authorized actions gated by Firebase auth + the owner-UID Firestore rules (Phase 5). No autonomous agent performs them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04 | Elevation of privilege | production deploy / live Firestore writes | low | mitigate | Kept as a blocking human checkpoint — never an autonomous task — so the outward-facing deploy and live-data seed stay owner-authorized (per the OWNER-DEFERRED decision). |
| T-11-SC | Tampering | supply chain (dependencies) | n/a | accept | No package installs; checkpoint-only plan. |
</threat_model>

<verification>
- Owner confirms both apps are live on darktierstudios.com (approved), or explicitly defers.
</verification>

<success_criteria>
- SC5 (owner-deferred): the site is deployed via `npm run deploy` and both apps are reachable live on
  darktierstudios.com — OR the owner has explicitly deferred with a standing checklist.
- SC4 (live side, owner): both production Firestore tool docs exist with slug matching the registry,
  status:"live", kind:"internal".
</success_criteria>

<output>
Create `.planning/phases/11-go-live-pipeline-docs/11-03-SUMMARY.md` when done.
</output>
