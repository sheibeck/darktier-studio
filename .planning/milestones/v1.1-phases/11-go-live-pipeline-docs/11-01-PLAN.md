---
phase: 11-go-live-pipeline-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/catalog/tools.ts
autonomous: true
requirements: [APP-01, APP-06]

must_haves:
  truths:
    - "The built /tools page lists Fate of the Fellowship and Burning Banners as live tools."
    - "Each app's Launch is a same-tab in-site link to /armory/<slug> (no target=_blank)."
    - "Every internal tool listed on the built /tools page points at a real, built /armory/<slug>.html route (no dead links)."
    - "Marketing pages (Home/Games/Tools) carry no companion-app JS and no Tailwind stylesheet — only the shared Nocturne Layout CSS and the pre-existing live-catalog islands."
    - "The site stays fully static (no output/adapter in astro.config.mjs) and both /armory routes appear in the sitemap."
  artifacts:
    - "src/data/catalog/tools.ts (two new status:live, kind:internal records)"
    - "dist/armory/fate-of-the-fellowship.html and dist/armory/burning-banners.html (built routes)"
    - "dist/sitemap-0.xml (both /armory routes present)"
  key_links:
    - "tools.ts internal slug === armoryApps registry slug === [slug].astro DISPATCHED_SLUGS === dist/armory/<slug>.html"
    - "ToolsLive internal branch → /armory/<slug> same-tab anchor rendered into dist/tools.html"
---

<objective>
Flip both companion apps live as discoverable Armory tools by adding two records to the canonical
seed catalog, then prove — against the actual build output — that the milestone adds zero regression
to the static, free-Spark marketing site.

Purpose: APP-01 (a visitor can open each app at its own in-site /armory URL from The Armory) and
APP-06 (the site stays fully static, free-Spark, with the public marketing pages still shipping no
companion-app JS). This plan delivers the code/seed side; the LIVE production Firestore docs and the
production deploy are owner actions handed off in 11-03.

Output: two live internal tool records in src/data/catalog/tools.ts, plus a verified green
seed-fallback build proving SC1 (built /tools lists both apps live with same-tab /armory links),
SC3 (marketing pages carry no companion-app bundles; static; free Spark) and the SC4 sitemap/shape
side.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-go-live-pipeline-docs/11-CONTEXT.md

# The exact files this plan seeds and cross-checks
@src/data/catalog/tools.ts
@src/lib/types.ts
@src/lib/armoryApps.ts
@src/pages/armory/[slug].astro
@src/components/live/ToolsLive.tsx
@src/lib/catalog.ts

# Interface facts the executor must honor:
# - Tool shape (src/lib/types.ts): { slug, name, status:"live"|"soon", hidden?, kind?:"external"|"internal", app?, kicker?, description, order }.
#   Internal tools OMIT `app` — ToolsLive derives the Launch href as `/armory/${slug}` for kind:"internal".
# - src/lib/armoryApps.ts registry slugs are EXACTLY: "fate-of-the-fellowship", "burning-banners".
# - src/pages/armory/[slug].astro has a build-time guard: DISPATCHED_SLUGS = new Set(["fate-of-the-fellowship","burning-banners"])
#   and one `{slug === "..." && <Component client:only="react" />}` dispatch line per app. It throws at build if a
#   registry entry lacks a dispatch line — but nothing guards a tools.ts internal record against a typo'd slug, which is
#   exactly the "listed tool points at a dead route" risk this plan verifies against the build output.
# - src/lib/catalog.ts getTools(): reads live Firestore via the Admin SDK when Firestore is configured
#   (serviceAccountKey.json present, or FIREBASE_* / GOOGLE_APPLICATION_CREDENTIALS / FIRESTORE_EMULATOR_HOST set),
#   otherwise falls back to the committed seed. The two records are the seed the owner later loads into production.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed Fate of the Fellowship and Burning Banners as live internal Armory tools</name>
  <files>src/data/catalog/tools.ts</files>
  <action>
Add two Tool records to the `tools` array in src/data/catalog/tools.ts, honoring the FIXED
pre-decided ordering: insert them among the live tools — after charlie-mike-toc (order 0) and
before the "docking soon" placeholders. Give Fate of the Fellowship order 1 and Burning Banners
order 2, then renumber the existing placeholders: table-utilities becomes order 3 and gm-tools
becomes order 4 (contiguous, no gaps or collisions).

Each new record MUST have: slug EXACTLY matching the registry/route slug — `fate-of-the-fellowship`
and `burning-banners` (verbatim, no trailing slash, lowercase) — `status: "live"`, `kind: "internal"`,
a short `kicker`, a `description`, and its `order`. Omit the `app` property entirely: internal tools
derive their in-site route from `slug` (`/armory/<slug>`), and setting an external URL would be wrong.
Do NOT invent a new data path — this canonical seed IS the source the owner loads into production
Firestore, and the build's seed fallback renders it.

Write the `name`/`kicker`/`description` copy at your discretion, aligned with the registry metadata
in src/lib/armoryApps.ts (e.g. name "Fate of the Fellowship" / "Burning Banners"; a "Live now"-worthy
one-line description of each companion). Keep the existing charlie-mike-toc, table-utilities and
gm-tools records otherwise unchanged apart from the two order renumbers. Preserve the file's existing
formatting style and the `import type { Tool }` header.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/data/catalog/tools.ts','utf8');for(const slug of ['fate-of-the-fellowship','burning-banners']){const m=s.match(new RegExp('\\\\{[^{}]*slug:\\\\s*\"'+slug+'\"[^{}]*\\\\}'));if(!m){console.error('MISSING record for '+slug);process.exit(1);}const b=m[0];if(!/status:\\s*\"live\"/.test(b)){console.error(slug+' not status:live');process.exit(1);}if(!/kind:\\s*\"internal\"/.test(b)){console.error(slug+' not kind:internal');process.exit(1);}if(/\\bapp:/.test(b)){console.error(slug+' must not set app');process.exit(1);}}console.log('seed shape OK');"</automated>
  </verify>
  <done>
tools.ts contains exactly two new records with slugs fate-of-the-fellowship and burning-banners,
each status:"live" + kind:"internal" and no `app` property; orders are contiguous 0..4 with the two
apps at 1 and 2; existing records preserved. The shape check passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Zero-regression seed-fallback build gate (SC1 + SC3 + SC4 sitemap/parity)</name>
  <files>src/data/catalog/tools.ts</files>
  <action>
Run a full `npm run build` that exercises the SEED fallback (not live Firestore), then assert against
the real build output. Because this repo builds against live Firestore whenever a service account is
present, the build must be run with Firestore disabled so getTools() renders the seed you just wrote —
this is also exactly the free, first-deploy / "starter catalog" state the phase delivers. Temporarily
relocate serviceAccountKey.json with a guaranteed restore (trap), unset the Firestore env vars for the
build only, then verify. Do NOT commit dist/ or the relocated key (both are gitignored).

Assertions the build output MUST satisfy:
- SC1: dist/tools.html contains a same-tab Launch link for each app — an anchor with href
  `/armory/fate-of-the-fellowship` and one with href `/armory/burning-banners`. Internal is the only
  code path in ToolsLive that emits an `/armory/` href (external tools emit a target=_blank external
  URL), so an `/armory/` href in the served HTML proves the same-tab in-site behavior by construction.
- Reachability/parity: dist/armory/fate-of-the-fellowship.html and dist/armory/burning-banners.html
  both exist, so no listed tool points at a dead route.
- SC3: dist/index.html, dist/games.html and dist/tools.html reference NO companion-app bundle — no
  BurningBanners / FateOfTheFellowship / lucide chunk, and no armory Tailwind stylesheet link
  (BurningBanners*.css / _slug_*.css). They keep only the shared Nocturne Layout stylesheet and the
  pre-existing live-catalog islands (this is what "~0KB app JS" means — the marketing pages never
  gain the companion-app JS/CSS).
- Static / free Spark: astro.config.mjs declares no `output` and no `adapter` key (comment lines
  excluded), so the site stays a pure static build with no SSR adapter.
- SC4 sitemap: dist/sitemap-0.xml includes both /armory/fate-of-the-fellowship and
  /armory/burning-banners.

The SC4 admin round-trip is structurally satisfied: the two records use only fields the admin CMS
already manages (slug, name, status, kind, description, order — all round-tripped in Phase 8), so no
new schema surface is introduced. The LIVE production Firestore docs are the owner action handed off
in 11-03.
  </action>
  <verify>
    <automated>bash -c 'set -e; KEY=serviceAccountKey.json; BAK="$KEY.plan11bak"; cleanup(){ [ -f "$BAK" ] && mv -f "$BAK" "$KEY"; }; trap cleanup EXIT; [ -f "$KEY" ] && mv -f "$KEY" "$BAK"; env -u FIREBASE_PROJECT_ID -u FIREBASE_SERVICE_ACCOUNT -u GOOGLE_APPLICATION_CREDENTIALS -u FIRESTORE_EMULATOR_HOST npm run build; grep -q "href=\"/armory/fate-of-the-fellowship\"" dist/tools.html; grep -q "href=\"/armory/burning-banners\"" dist/tools.html; test -f dist/armory/fate-of-the-fellowship.html; test -f dist/armory/burning-banners.html; for f in dist/index.html dist/games.html dist/tools.html; do if grep -Eq "BurningBanners|FateOfTheFellowship|lucide|_slug_[.-][A-Za-z0-9]+\.css" "$f"; then echo "REGRESSION: companion-app bundle leaked into $f"; exit 1; fi; done; if grep -v "^[[:space:]]*//" astro.config.mjs | grep -Eq "^[[:space:]]*(output|adapter):"; then echo "NOT STATIC: output/adapter present"; exit 1; fi; grep -q "armory/fate-of-the-fellowship" dist/sitemap-0.xml; grep -q "armory/burning-banners" dist/sitemap-0.xml; echo "ZERO-REGRESSION BUILD OK"'</automated>
  </verify>
  <done>
The seed-fallback `npm run build` succeeds. dist/tools.html lists both apps as live tools with
same-tab `/armory/<slug>` Launch links; both dist/armory/<slug>.html routes exist (no dead links);
Home/Games/Tools carry no companion-app JS or Tailwind CSS; astro.config.mjs has no output/adapter
(fully static, free Spark); dist/sitemap-0.xml lists both /armory routes. serviceAccountKey.json is
restored and dist/ is left uncommitted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner → Firestore `tools` collection | Tool records are owner-controlled content; production writes are gated by the existing owner-UID Firestore rules (Phase 5). This plan only edits the committed seed (a source file in the repo), not live data. |
| build → static `dist/` | The build reads catalog data (seed or Firestore) and emits static HTML; no per-request server, no user input crosses into the build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | src/data/catalog/tools.ts seed records | low | accept | Records are owner-authored source content; production Firestore writes remain gated by the existing owner-UID rules. No new write path or input surface is added. |
| T-11-02 | Spoofing/Info-disclosure | internal Launch link target | low | mitigate | Slug-parity is verified against the real build output (every listed `/armory/*` href resolves to a built dist/armory/<slug>.html), so a mistyped slug cannot ship a dead/misdirected Launch link. |
| T-11-SC | Tampering | supply chain (dependencies) | n/a | accept | No package installs in this plan (no new dependencies this phase). |
</threat_model>

<verification>
- `node` seed-shape check passes (both records live + internal, no `app`).
- Seed-fallback `npm run build` succeeds and all SC1/SC3/SC4-sitemap greps pass.
- serviceAccountKey.json present again after the build; no dist/ or key staged for commit.
</verification>

<success_criteria>
- APP-01: the built /tools page lists both apps as live and each Launch is a same-tab `/armory/<slug>`
  link resolving to a real built route.
- APP-06: Home/Games/Tools ship no companion-app JS/CSS; the site stays fully static (no output/adapter),
  free Spark, with no new recurring cost.
- SC4 (code/seed side): both records carry slug matching the registry exactly, status:"live",
  kind:"internal", use only admin-managed fields, and both /armory routes are in the sitemap.
</success_criteria>

<output>
Create `.planning/phases/11-go-live-pipeline-docs/11-01-SUMMARY.md` when done.
</output>