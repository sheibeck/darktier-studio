---
phase: 10-burning-banners
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - src/components/armory/BurningBanners.tsx
  - src/styles/armory-bb-tailwind.css
autonomous: false
requirements: [BB-01, BB-02]
user_setup: []

must_haves:
  truths:
    - "A human confirms Burning Banners plays at /armory/burning-banners: basic/advanced switch, campaign setup, and trackers all work."
    - "A human confirms save persists across a reload and the Abandon button resets the campaign."
    - "A human confirms the Nocturne nav/footer/buttons look identical on the BB page and on Home/Games/Tools (no visible reset leak)."
    - "A human confirms the app keeps its own dark look (no accidental Nocturne restyle) and any bare-button chrome gap is accepted or fixed with a scoped .bb-app override — never a global preflight import."
  artifacts:
    - dist/armory/burning-banners.html
  key_links:
    - "Human visual verification closes the gap the automated gates cannot: rendered fidelity + interactive correctness in a real browser."
---

<objective>
Blocking human play-through of Burning Banners on a real preview build — the one gate automated checks cannot cover (visual fidelity of the Nocturne shell + interactive correctness of the app). This plan is NON-AUTONOMOUS and will be DEFERRED in the autonomous run; the owner runs it end-of-milestone.

Purpose: Confirm BB-01 (playable + persistence) and the visual half of BB-02 (Nocturne chrome unbroken, no preflight leak visible) with human eyes. Provides the documented decision point for the one residual risk research flagged: bare-<button> browser chrome showing through where the skip-preflight approach applied no reset. If a gap is found, it is fixed with a scoped .bb-app override (or accepted) — NEVER by importing global preflight.

Output: An owner "approved" (or a listed set of issues to fix). If a scoped chrome fix is needed, a 2-3 rule .bb-app override added under the app's root wrapper in the BB stylesheet — kept scoped, never global.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/10-burning-banners/10-CONTEXT.md
@.planning/research/STACK.md
@src/components/armory/BurningBanners.tsx
@src/styles/armory-bb-tailwind.css
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Human play-through of Burning Banners (deferred, owner)</name>
  <action>
  DEFERRED in this autonomous run (non-blocking, per the run directive to batch all human verification to end-of-milestone). The owner performs a browser play-through end-of-milestone: build + `npm run preview`, open /armory/burning-banners, and run the six checks in <how-to-verify>. If a bare-button chrome gap is found, fix it with 2-3 CSS rules scoped under a `.bb-app` wrapper (in `armory-bb-tailwind.css` / the app root) — NEVER a global preflight import. This checkpoint is NOT executed by an agent; it is recorded as a pending owner action.
  </action>
  <what-built>
  Burning Banners is live at /armory/burning-banners as a client:only React island inside the Nocturne shell, with Tailwind v4 + lucide-react scoped to this route via a skip-preflight stylesheet, real localStorage persistence (key bb:campaign), and one registry entry + dispatch line. Automated gates (plans 10-01/10-02) already proved: build succeeds, zero new script/link tags on marketing pages, no preflight anywhere in the build, no Tailwind/lucide on marketing assets, per-app SEO + sitemap, and clean dependency resolution. What remains is human confirmation of rendered look and interactive behavior.
  </what-built>
  <how-to-verify>
  1. Serve the production build locally: run `npm run build` then `npm run preview`, and open the previewed /armory/burning-banners URL.
  2. Playability (BB-01): the app renders with its own dark look. Toggle BASIC/ADVANCED and confirm the mode switches. Go to Setup, create a campaign, and confirm the Turn tracker appears with income/revolt/coven/collapse (and, in Advanced, arcane study + winter) controls. Open Combat and Rules (reference) tabs and confirm they render.
  3. Persistence (BB-01): with a campaign in progress, reload the page — the campaign should restore (view returns to the turn tracker). Then click "Abandon this campaign and set up a new one" and confirm state clears and does not come back after another reload.
  4. Nocturne shell intact (BB-02, visual): on the BB page, confirm the site Nav (top) and Footer render exactly as on other pages — filled buttons, correct radii, spacing, and the accent color are all present, NOT stripped/flattened. Then open Home (/), Games (/games), and Tools (/tools) and confirm their nav/footer/buttons/headings look identical to before this phase (no reset leaked).
  5. Own look preserved (BB-02): confirm the app was NOT accidentally restyled toward Nocturne — it keeps its own palette and inline-styled primitives.
  6. Bare-button chrome check (research Q2 residual risk): look for any bare <button> inside the app showing default browser chrome (unexpected native border/background). If found: this is fixed with 2-3 CSS rules scoped under a .bb-app wrapper class added to the app root in BurningBanners.tsx and armory-bb-tailwind.css — NEVER by importing global preflight. If none found, the skip-preflight approach is accepted as-is.
  </how-to-verify>
  <resume-signal>Type "approved" if all six checks pass, or list the specific issues (which check, what you saw) to fix. Any chrome-gap fix must stay scoped under .bb-app — never a global preflight import.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rendered UI → human judgment | Visual fidelity of the Nocturne chrome and the app's own look can only be confirmed by a human; automated gates cover bytes, not appearance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-07 | Tampering | Visible reset leak or broken app not caught by automated gates | medium | mitigate | Blocking human play-through across the BB page + all three marketing pages; any gap fixed with a scoped .bb-app override, never global preflight. |
</threat_model>

<verification>
- Owner returns "approved", confirming playability, persistence, unbroken Nocturne chrome, and preserved app look.
- Any reported chrome gap is fixed with a scoped .bb-app override (or explicitly accepted) — global preflight is never introduced.
</verification>

<success_criteria>
- Burning Banners is confirmed playable with working save + reset in a real browser (BB-01).
- The Nocturne nav/footer/buttons are confirmed visually unchanged on the BB page and on Home/Games/Tools (BB-02).
- Any bare-button chrome gap is accepted or fixed with a scoped fallback — never a global preflight import (BB-02).
</success_criteria>

<output>
Create `.planning/phases/10-burning-banners/10-03-SUMMARY.md` when done.
</output>
