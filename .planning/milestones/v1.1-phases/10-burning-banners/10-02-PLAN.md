---
phase: 10-burning-banners
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified: []
autonomous: true
requirements: [BB-01, BB-02]
user_setup: []

must_haves:
  truths:
    - "Tailwind preflight/reset exists nowhere in the emitted build, so no marketing page's nav/footer/buttons were reset."
    - "No asset loaded by Home/Games/Tools contains Tailwind utility CSS or lucide-react code."
    - "The BB Tailwind stylesheet is imported only from BurningBanners.tsx and omits the bundled Tailwind entrypoint."
    - "dist/armory/burning-banners.html carries a per-app Burning Banners <title>/description (not the site defaults) and the route is in the sitemap."
    - "npm ls resolves lucide-react/tailwindcss/@tailwindcss/vite cleanly (no ERESOLVE / no unmet peers) and no SSR adapter was added."
  artifacts:
    - dist/armory/burning-banners.html
    - dist/sitemap-0.xml
  key_links:
    - "Marketing-page assets carry zero Tailwind/lucide bytes -> the scoping mechanism (skip-preflight, component-scoped import, island-only lucide) actually held in the emitted output."
---

<objective>
Prove — with real, executable gates against the emitted build and the source — that Burning Banners' Tailwind v4 + lucide-react are scoped entirely to /armory/burning-banners and that the milestone's headline no-regression constraint (BB-02) holds beyond the tag-diff already run in 10-01.

Purpose: Close BB-02 with defense-in-depth: preflight literally absent from the build, no Tailwind/lucide bytes on any marketing asset, the stylesheet imported only from the BB component with preflight omitted, per-app SEO + sitemap present, and a clean dependency resolution against React 19. Nothing here restyles or edits the app — it only inspects and asserts.

Output: Two automated gate tasks (built-output isolation + source-level scoping/dependency health) that pass, confirming the scoping decision from research/STACK.md is real in the shipped output, not just intended.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/10-burning-banners/10-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

@src/components/armory/BurningBanners.tsx
@src/styles/armory-bb-tailwind.css
@src/layouts/Layout.astro
@src/styles/site.css
@src/styles/nocturne.css
@astro.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Built-output isolation gates — preflight absent, no Tailwind/lucide on marketing assets, per-app SEO + sitemap</name>
  <files>(read-only verification against dist/)</files>
  <action>
  Run `npm run build` to guarantee a fresh dist/, then assert the following against the emitted output. These prove BB-02 in the shipped bytes (complementing the tag-diff already run in 10-01):

  1. Preflight absent from the ENTIRE build: no emitted CSS contains Tailwind's preflight fingerprints. The skip-preflight import means the global reset is never generated anywhere, so it cannot have touched the shared nav/footer/buttons on any page. Assert no *.css under dist/ contains the html text-size-adjust reset, and none contains the preflight universal-reset pseudo-element selector.

  2. No Tailwind or lucide bytes on marketing assets: for every .css/.js asset referenced by dist/index.html, dist/games.html, dist/tools.html, assert the file contains no Tailwind utility class definitions (e.g. min-h-screen, max-w-4xl) and no lucide code. Note that /games and /tools legitimately ship their own VaultLive/ToolsLive React islands and the shared React runtime chunk — those are expected and carry neither Tailwind utilities nor lucide, so they pass; the BB island JS (which carries lucide) and the BB Tailwind CSS are simply never referenced by these pages.

  3. Positive control — the BB route DID get its scoped Tailwind: assert dist/armory/burning-banners.html references a stylesheet that contains a Tailwind utility class definition (proves the scoped skip-preflight stylesheet actually compiled and shipped for this route).

  4. Per-app SEO: assert dist/armory/burning-banners.html has a <title> containing "Burning Banners" (the registry title, not the site default) and a meta description present in the initial HTML.

  5. Sitemap crawlability: assert dist/sitemap-0.xml contains the /armory/burning-banners route (the sitemap filter only excludes /admin, so this route is included automatically — confirm it is).
  </action>
  <verify>
    <automated>npm run build && ! grep -rlq --include='*.css' 'text-size-adjust' dist/ && ! grep -rlq --include='*.css' '::backdrop' dist/ && for P in index games tools; do for A in $(grep -oE '(src|href)="/[^"]*\.(css|js)"' "dist/$P.html" | grep -oE '/[^"]*\.(css|js)'); do FP="dist${A}"; if [ -f "$FP" ]; then grep -qE 'min-h-screen|max-w-4xl|text-size-adjust' "$FP" && exit 1; grep -q 'lucide' "$FP" && exit 1; fi; done; done; BBHIT=0; for A in $(grep -oE 'href="/[^"]*\.css"' dist/armory/burning-banners.html | grep -oE '/[^"]*\.css'); do grep -qE 'min-h-screen|max-w-4xl' "dist${A}" && BBHIT=1; done; [ "$BBHIT" -eq 1 ] && grep -qE '<title>[^<]*Burning Banners' dist/armory/burning-banners.html && grep -q '<meta name="description"' dist/armory/burning-banners.html && grep -q 'armory/burning-banners' dist/sitemap-0.xml</automated>
  </verify>
  <done>Fresh build has no preflight fingerprint anywhere; no marketing-page asset carries Tailwind utilities or lucide; the BB route ships a Tailwind-utility stylesheet; burning-banners.html has a Burning Banners <title> + meta description; the route is in sitemap-0.xml.</done>
</task>

<task type="auto">
  <name>Task 2: Source-level scoping + dependency-health gates</name>
  <files>(read-only verification against source + node_modules)</files>
  <action>
  Assert the scoping mechanism is correct at the source level and the dependency tree is clean — these prevent a future edit from silently reintroducing the leak:

  1. Component-scoped import only: src/styles/armory-bb-tailwind.css is imported from src/components/armory/BurningBanners.tsx and from NOWHERE else — specifically NOT from src/layouts/Layout.astro, src/styles/site.css, or src/styles/nocturne.css (grep those three files for the stylesheet name and assert absent). This is the single structural fact that keeps Tailwind out of every non-BB page.

  2. Skip-preflight shape: armory-bb-tailwind.css imports the theme and utilities sub-path layers and includes an @source line, and does NOT contain the bundled Tailwind shorthand entrypoint (the import with no /theme.css or /utilities.css sub-path) — the shorthand is what would pull in preflight.

  3. lucide named-imports only: BurningBanners.tsx imports lucide icons as a named destructured block and contains no namespace/barrel import from lucide-react anywhere in src/ (a barrel import would defeat tree-shaking and bloat the island).

  4. Dependency health: `npm ls lucide-react tailwindcss @tailwindcss/vite` exits 0 (all present, no unmet/invalid peer dependencies) — this is the standing confirmation that React 19 produced no ERESOLVE conflict.

  5. Still static / free Spark: astro.config.mjs has no output or adapter key (no SSR adapter, no Cloud Functions were introduced by the Tailwind/lucide addition).
  </action>
  <verify>
    <automated>head -25 src/components/armory/BurningBanners.tsx | grep -q 'armory-bb-tailwind.css' && ! grep -q 'armory-bb-tailwind' src/layouts/Layout.astro && ! grep -q 'armory-bb-tailwind' src/styles/site.css && ! grep -q 'armory-bb-tailwind' src/styles/nocturne.css && grep -q 'tailwindcss/theme.css' src/styles/armory-bb-tailwind.css && grep -q 'tailwindcss/utilities.css' src/styles/armory-bb-tailwind.css && grep -q '@source' src/styles/armory-bb-tailwind.css && ! grep -Eq '@import[[:space:]]+"tailwindcss"[[:space:]]*;' src/styles/armory-bb-tailwind.css && grep -Eq 'import[[:space:]]*\{[^}]*\}[[:space:]]*from[[:space:]]*"lucide-react"' src/components/armory/BurningBanners.tsx && [ "$(grep -rlE 'import[[:space:]]+\*[[:space:]]+as[[:space:]]+[A-Za-z_]+[[:space:]]+from[[:space:]]+"lucide-react"' src/ | wc -l)" -eq 0 ] && npm ls lucide-react tailwindcss @tailwindcss/vite >/dev/null 2>&1 && ! grep -Eq '^[[:space:]]*(output|adapter)[[:space:]]*:' astro.config.mjs</automated>
  </verify>
  <done>armory-bb-tailwind.css is imported only from BurningBanners.tsx (never Layout/site/nocturne); the stylesheet uses theme+utilities layers with an @source and no bundled entrypoint; lucide is named-imported with no barrel import in src/; npm ls resolves the 3 packages cleanly; astro.config.mjs has no output/adapter key.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BB island CSS/JS → shared marketing bundles | The isolation boundary this plan audits: Tailwind/lucide must be absent from every asset the marketing pages load. |
| Source import graph → build output | A single stray import (stylesheet from Layout, or a lucide barrel) would silently reintroduce the leak; source-level gates lock this. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Tampering/Info | Preflight/reset or Tailwind utilities leaking onto a marketing page undetected | high | mitigate | Task 1 asserts no preflight fingerprint anywhere in dist/ and no Tailwind/lucide bytes on any marketing-referenced asset — a real executable gate over the emitted output, not an assertion. |
| T-10-06 | Tampering | Future edit re-imports the stylesheet globally or adds a lucide barrel | medium | mitigate | Task 2 pins the source-level facts (single import site, skip-preflight shape, named lucide only) so a regression fails this gate. |
| T-10-SC | Tampering (supply chain) | lucide-react/tailwindcss/@tailwindcss/vite resolution vs React 19 | high | mitigate | Task 2 `npm ls` confirms clean resolution (no ERESOLVE); versions verified against npm registry in research/STACK.md (all [OK]). |
</threat_model>

<verification>
- Both tasks' automated gates pass on a fresh `npm run build`.
- BB-02 is proven from three independent angles: 10-01's zero-new-tags baseline diff, 10-02 Task 1's preflight-absent + no-Tailwind/lucide-on-marketing-assets, and 10-02 Task 2's source-level scoping facts.
- BB-01's build/SEO/sitemap/persistence-shape are confirmed; interactive playability is verified by the human play-through in plan 10-03 (deferred in the autonomous run).
</verification>

<success_criteria>
- No Tailwind preflight/reset exists anywhere in the emitted build (BB-02).
- No marketing-page asset carries Tailwind utilities or lucide-react (BB-02).
- The BB Tailwind stylesheet is imported only from BurningBanners.tsx with preflight omitted; lucide is named-imported only (BB-02).
- burning-banners.html has per-app SEO and is in the sitemap; the 3 packages resolve with no ERESOLVE; the site stays static (BB-01, BB-02).
</success_criteria>

<output>
Create `.planning/phases/10-burning-banners/10-02-SUMMARY.md` when done.
</output>
