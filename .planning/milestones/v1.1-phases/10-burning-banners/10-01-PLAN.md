---
phase: 10-burning-banners
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - astro.config.mjs
  - src/styles/armory-bb-tailwind.css
  - src/components/armory/BurningBanners.tsx
  - apps/bb-companion.tsx
  - src/lib/armoryApps.ts
  - src/pages/armory/[slug].astro
autonomous: true
requirements: [BB-01, BB-02]
user_setup: []

must_haves:
  truths:
    - "Visiting /armory/burning-banners loads Burning Banners (basic/advanced switch + trackers) keeping its own dark look."
    - "A campaign persists in real browser localStorage under key bb:campaign across reloads; the Abandon button clears it."
    - "npm run build succeeds and emits dist/armory/burning-banners.html."
    - "npm install resolves lucide-react + tailwindcss + @tailwindcss/vite with no ERESOLVE against React 19."
    - "Home/Games/Tools emit zero new <script>/<link> tags versus the pre-phase baseline."
  artifacts:
    - src/components/armory/BurningBanners.tsx
    - src/styles/armory-bb-tailwind.css
    - dist/armory/burning-banners.html
  key_links:
    - "armoryApps.ts entry (slug burning-banners) -> [slug].astro dispatch line + DISPATCHED_SLUGS entry (parity guard passes)."
    - "BurningBanners.tsx imports src/styles/armory-bb-tailwind.css (component-scoped, never from Layout/site/nocturne)."
    - "@tailwindcss/vite registered in astro.config.mjs vite.plugins compiles the skip-preflight stylesheet only for the BB route."
---

<objective>
Ship Burning Banners live at /armory/burning-banners by extending the proven Phase-9 app-hosting pattern to a heavier app that needs Tailwind v4 + lucide-react — scoped ENTIRELY to its own route so no Nocturne marketing page changes by a single byte.

Purpose: Deliver BB-01 (playable, localStorage save + reset) and BB-02 (Tailwind/lucide scoped so the global preflight/reset never reaches the shared nav/footer/buttons). This is the milestone's real risk; the skip-preflight scoping decision (research/STACK.md) makes cross-page AND same-page reset leakage structurally impossible.

Output: The 3 new packages installed, @tailwindcss/vite wired, a scoped skip-preflight BB stylesheet, the relocated + storage-migrated + mounted BB island, one registry entry + one dispatch line satisfying the build-time parity guard, and a passing npm run build whose marketing-page output is byte-stable versus a captured baseline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Binding pre-decided architecture for this phase
@.planning/phases/10-burning-banners/10-CONTEXT.md

# THE Tailwind v4 skip-preflight decision + exact import lines + versions
@.planning/research/STACK.md
# Pitfall 1 (preflight leak), Pitfall 4 (lucide), Pitfall 5 (bundle)
@.planning/research/PITFALLS.md
# Patterns 1-3 (registry route, client:only, scoped Tailwind)
@.planning/research/ARCHITECTURE.md

# The Phase-9 pattern to mirror exactly
@.planning/phases/09-fate-of-the-fellowship/09-01-SUMMARY.md
@src/components/armory/FateOfTheFellowship.tsx
@src/lib/armoryApps.ts
@src/pages/armory/[slug].astro
@astro.config.mjs

# The app being onboarded
@apps/bb-companion.tsx
</context>

<!-- planner-discipline-allow: window.storage -->

<tasks>

<task type="auto">
  <name>Task 1: Capture BB-02 baseline, then add deps + wire @tailwindcss/vite + create the scoped skip-preflight stylesheet</name>
  <files>package.json, package-lock.json, astro.config.mjs, src/styles/armory-bb-tailwind.css</files>
  <action>
  FIRST, before installing anything or editing any file, capture the marketing-page baseline that BB-02's no-regression gate compares against. Run `npm run build` on the untouched tree, create directory `.planning/phases/10-burning-banners/.bb02-baseline/`, and for each of dist/index.html, dist/games.html, dist/tools.html extract every script/link tag with `grep -oE '<(script|link)\b[^>]*>' dist/PAGE.html | sort > .planning/phases/10-burning-banners/.bb02-baseline/PAGE.tags.txt`. These three baseline files record the exact set of tags each marketing page shipped BEFORE Tailwind/lucide existed. (This directory is a throwaway verification artifact.)

  THEN install the three researched packages at the versions verified in research/STACK.md against the npm registry (2026-08-18): `npm install lucide-react` (runtime dependency, shipped inside the island) and `npm install -D tailwindcss @tailwindcss/vite` (build-time devDependencies). Capture the install output and confirm it reports no ERESOLVE peer conflict — React 19.2.8 satisfies lucide-react's peer range (^16.5.1 || ^17.0.0 || ^18.0.0 || ^19.0.0), and @tailwindcss/vite's vite peer range covers Astro 7.2.2's bundled Vite. Keep tailwindcss and @tailwindcss/vite pinned to the same version (they ship as a matched pair). Package legitimacy: all three are verified-legitimate against the primary source (npm registry) with confirmed publishers (Tailwind Labs, lucide-icons) in research/STACK.md — treat as [OK], no human verification checkpoint required.

  THEN register the Tailwind Vite plugin in astro.config.mjs: add the import `import tailwindcss from "@tailwindcss/vite";` at the top alongside the other integration imports, and add a top-level `vite: { plugins: [tailwindcss()] }` key to the defineConfig object (the config currently has no vite key). Do NOT use the deprecated @astrojs/tailwind integration and do NOT add it to the integrations array. Leave the integrations array (sitemap, icon, react) and the react include glob unchanged — the react glob already covers **/components/armory/** from Phase 9. Do NOT add any output or adapter key (the site stays implicitly static on the free Spark plan).

  THEN create src/styles/armory-bb-tailwind.css as the Burning-Banners-only stylesheet using the exact skip-preflight pattern from research/STACK.md. Its four lines are, in order: a layer declaration line `@layer theme, utilities;`, then `@import "tailwindcss/theme.css" layer(theme);`, then `@import "tailwindcss/utilities.css" layer(utilities);`, then a source-scoping line `@source "../components/armory/BurningBanners.tsx";` (path is relative to this stylesheet's location in src/styles/, pointing at the relocated component created in Task 2). Import ONLY the theme and utilities sub-path layers; the preflight/global-reset layer is deliberately omitted so no browser reset exists anywhere in the build. Do NOT add the bundled shorthand Tailwind entrypoint (the import without a /theme.css or /utilities.css sub-path) — that is what pulls in preflight and is forbidden here. This stylesheet must be imported ONLY from BurningBanners.tsx (Task 2) — never from Layout.astro, site.css, or nocturne.css.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');const d={...p.dependencies,...p.devDependencies};if(!d['lucide-react']||!d['tailwindcss']||!d['@tailwindcss/vite'])process.exit(1)" && npm ls lucide-react tailwindcss @tailwindcss/vite >/dev/null 2>&1 && grep -q '@tailwindcss/vite' astro.config.mjs && grep -q 'tailwindcss/theme.css' src/styles/armory-bb-tailwind.css && grep -q 'tailwindcss/utilities.css' src/styles/armory-bb-tailwind.css && grep -q '@source' src/styles/armory-bb-tailwind.css && ! grep -Eq '@import[[:space:]]+"tailwindcss"[[:space:]]*;' src/styles/armory-bb-tailwind.css && ! grep -q 'output' astro.config.mjs && test -f .planning/phases/10-burning-banners/.bb02-baseline/index.tags.txt</automated>
  </verify>
  <done>Baseline tag lists for index/games/tools captured; lucide-react, tailwindcss, @tailwindcss/vite installed with no ERESOLVE; @tailwindcss/vite registered in astro.config.mjs vite.plugins; armory-bb-tailwind.css exists importing only theme + utilities layers with an @source line and no bundled/bare Tailwind entrypoint; no output/adapter key added.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Relocate the BB app, migrate storage to real localStorage, import the scoped stylesheet, confirm named lucide imports</name>
  <files>src/components/armory/BurningBanners.tsx, apps/bb-companion.tsx, src/styles/armory-bb-tailwind.css</files>
  <action>
  Relocate the app with `git mv apps/bb-companion.tsx src/components/armory/BurningBanners.tsx` (a pure move — the default export `App` and all internal logic/look stay unchanged, mirroring how Phase 9 moved fotf-companion.tsx to FateOfTheFellowship.tsx). The astro.config react include glob already matches src/components/armory/**, so no config change is needed here.

  At the very top of the relocated BurningBanners.tsx, add the stylesheet import `import "../../styles/armory-bb-tailwind.css";` (relative path from src/components/armory/ to src/styles/). This is the ONLY place the BB Tailwind stylesheet is ever imported — it keeps Tailwind's utility CSS code-split to this island's own route.

  Migrate persistence from the Claude-artifact storage shim to real browser localStorage, keeping the SAME key "bb:campaign" and the SAME serialized shape (JSON.stringify(game)). There are exactly three call sites (currently the artifact shim accessed as window.storage — see reference lines below), all already inside effects/handlers, so no restructuring is needed; just replace the transport:
    - Load effect (~line 2517): the shim read `const r = await window.storage.get("bb:campaign")` returned a wrapper object read as `r.value`. Replace with a synchronous real read: `const raw = window.localStorage.getItem("bb:campaign")` and update the guard to `if (!cancelled && raw) { setGame(JSON.parse(raw)); setView("turn"); }`. Drop the now-unnecessary await and the .value unwrap (getItem returns the stored string directly). Keep the surrounding try/catch and cancelled guard.
    - Save effect (~line 2527): replace `await window.storage.set("bb:campaign", JSON.stringify(game))` with `window.localStorage.setItem("bb:campaign", JSON.stringify(game))`.
    - Reset handler (~line 2636, the Abandon-campaign button onClick): replace `window.storage.delete("bb:campaign")` with `window.localStorage.removeItem("bb:campaign")`.
  Mirror FateOfTheFellowship.tsx which uses window.localStorage.getItem/setItem inside useEffect. All browser-API access must remain inside effects/handlers — NO module-top-level window/localStorage/document access, because armoryApps.ts imports this component at build time in Node (top-level code runs in the Node build). The existing module-top-level code is pure data + helper functions only, so leave it as-is.

  Confirm the lucide-react icons stay imported by name only (the file already imports the ~29 icons as a named destructured block `import { Swords, Coins, ... } from "lucide-react";` at the top). Do NOT convert this to a namespace/barrel form and do NOT add one elsewhere — named imports are what let Vite tree-shake to only the icons actually used, keeping the island bundle to tens of KB rather than the full icon set. The app is island-only (client:only, wired in Task 3) so lucide never touches marketing-page JS.
  </action>
  <verify>
    <automated>test -f src/components/armory/BurningBanners.tsx && ! test -f apps/bb-companion.tsx && head -20 src/components/armory/BurningBanners.tsx | grep -q 'armory-bb-tailwind.css' && grep -q 'localStorage.getItem("bb:campaign")' src/components/armory/BurningBanners.tsx && grep -q 'localStorage.setItem("bb:campaign"' src/components/armory/BurningBanners.tsx && grep -q 'localStorage.removeItem("bb:campaign")' src/components/armory/BurningBanners.tsx && [ "$(grep -c 'window\.storage' src/components/armory/BurningBanners.tsx)" -eq 0 ] && grep -Eq 'import[[:space:]]*\{[^}]*Swords' src/components/armory/BurningBanners.tsx && ! grep -Eq 'import[[:space:]]+\*[[:space:]]+as[[:space:]]+[A-Za-z]+[[:space:]]+from[[:space:]]+"lucide-react"' src/components/armory/BurningBanners.tsx</automated>
  </verify>
  <done>apps/bb-companion.tsx is gone and src/components/armory/BurningBanners.tsx exists (git mv); it imports armory-bb-tailwind.css at the top; all three storage sites use window.localStorage (getItem/setItem/removeItem) under key bb:campaign with the artifact shim fully removed; lucide icons remain named imports with no barrel/namespace import; no module-top-level browser access introduced.</done>
</task>

<task type="auto">
  <name>Task 3: Register BB in the pipeline (entry + dispatch + parity guard), build, and prove zero marketing-page regression</name>
  <files>src/lib/armoryApps.ts, src/pages/armory/[slug].astro</files>
  <action>
  Add Burning Banners to the registry and route mirroring the existing Fate of the Fellowship wiring exactly (this is the "~3 mechanical lines per app" pipeline proven in Phase 9 — see 09-01-SUMMARY.md).

  In src/lib/armoryApps.ts: add `import BurningBanners from "../components/armory/BurningBanners";` next to the FateOfTheFellowship import, and append one ArmoryApp entry to the armoryApps array with slug "burning-banners", name "Burning Banners", a per-app title (e.g. "Burning Banners — Table Companion"), a per-app description summarizing the tool (basic/advanced game, setup helper, turn/income/revolt/coven/collapse trackers, combat and rules reference — this becomes the page title/description/OG and the static crawlable intro), and Component: BurningBanners. The slug MUST be exactly "burning-banners" (it must match the future Firestore tools/{slug} doc that Phase 11 creates).

  In src/pages/armory/[slug].astro: add `import BurningBanners from "../../components/armory/BurningBanners";` next to the FateOfTheFellowship import; add "burning-banners" to the DISPATCHED_SLUGS set; and add one dispatch line inside the .armory-app-shell div mirroring the existing one: `{slug === "burning-banners" && <BurningBanners client:only="react" />}`. Use client:only="react" (no SSR value — the app is entirely localStorage-driven, same as FOTF and AdminApp). The build-time parity guard in this file throws if a registry entry lacks a matching DISPATCHED_SLUGS dispatch line — adding both the set entry and the JSX line satisfies it. Do NOT thread the component through getStaticPaths props or a Record lookup (Phase 9 proved both fail Astro's client:only static analysis with NoMatchingImport — see the frontmatter comment in [slug].astro). Do NOT edit getStaticPaths, Layout usage, or the static intro section — they are already generic and pick up the new registry entry automatically.

  THEN run `npm run build` (the hard gate). It must succeed and emit dist/armory/burning-banners.html. If the parity guard throws, a dispatch line is missing — fix it. If the build throws localStorage/window is not defined, a browser access escaped an effect in Task 2 — fix it.

  THEN prove BB-02's headline no-regression: re-extract the script/link tags from the freshly built marketing pages and diff each against the Task-1 baseline. For each of index, games, tools: `grep -oE '<(script|link)\b[^>]*>' dist/PAGE.html | sort` must be byte-identical to .planning/phases/10-burning-banners/.bb02-baseline/PAGE.tags.txt (zero added or changed tags). Any new <script>/<link> — or a changed asset hash on an existing tag — means Tailwind/lucide leaked into a shared chunk; investigate and fix (it must not). A clean diff is the proof that adding Tailwind + lucide-react changed nothing on Home/Games/Tools.
  </action>
  <verify>
    <automated>npm run build && test -f dist/armory/burning-banners.html && grep -q 'burning-banners' src/lib/armoryApps.ts && grep -q 'burning-banners' src/pages/armory/'[slug].astro' && for P in index games tools; do grep -oE '<(script|link)\b[^>]*>' "dist/$P.html" | sort > "/tmp/bb02-$P.after.txt"; diff ".planning/phases/10-burning-banners/.bb02-baseline/$P.tags.txt" "/tmp/bb02-$P.after.txt" || exit 1; done</automated>
  </verify>
  <done>armoryApps.ts has a burning-banners entry importing BurningBanners; [slug].astro has the matching DISPATCHED_SLUGS entry + `{slug === "burning-banners" && <BurningBanners client:only="react" />}` dispatch line and the parity guard passes; npm run build succeeds and emits dist/armory/burning-banners.html; the script/link tag lists for dist/index.html, dist/games.html, dist/tools.html are byte-identical to the pre-phase baseline (zero new or changed tags).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build pipeline | 3 new third-party packages (lucide-react, tailwindcss, @tailwindcss/vite) enter the build; untrusted code runs at install/build time. |
| BB island CSS/JS → shared marketing bundles | The isolation boundary that MUST hold: Tailwind utility CSS + lucide JS must stay code-split to the /armory/burning-banners route and never enter Home/Games/Tools chunks. |
| Browser → localStorage | The app reads/writes the user's own browser storage (key bb:campaign); no cross-user, no server, no auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering/Info | Tailwind global preflight/reset leaking into Nocturne nav/footer/buttons (the #1 phase risk) | high | mitigate | Skip-preflight scoped stylesheet: import ONLY theme + utilities layers (research/STACK.md), never the bundled entrypoint; @source scoped to BurningBanners.tsx; imported only from the BB component, never from Layout/site/nocturne. Preflight never exists in the build. Verified by the baseline tag-diff (this plan, Task 3) + preflight-absent grep (plan 10-02). |
| T-10-02 | Info/Tampering | BB Tailwind CSS or lucide-react JS pulled into a shared marketing chunk | medium | mitigate | Component-scoped import graph (island-only, client:only), per-route code-split. Verified in 10-02 that BB assets are referenced only by burning-banners.html. |
| T-10-SC | Tampering (supply chain) | lucide-react@1.32.0, tailwindcss@4.3.3, @tailwindcss/vite@4.3.3 npm installs | high | mitigate | Versions verified against the primary source (npm registry, 2026-08-18) with confirmed publishers (Tailwind Labs, lucide-icons) in research/STACK.md — all three [OK], not [ASSUMED], so no blocking human-verify checkpoint. Lockfile-pinned; named lucide imports only (no barrel); no output/adapter added; `npm ls` confirms clean resolution with no ERESOLVE vs React 19. |
| T-10-03 | Denial of Service (deploy) | Module-top-level or render-time localStorage/window access breaking astro build | medium | mitigate | All browser-API access confined to useEffect/handlers (Task 2); registry imports the component in Node at build time, so top-level stays browser-free. Build gate (Task 3) fails loudly if violated. |
| T-10-04 | Spoofing/Repudiation/Elevation | Auth / cross-user state | low | accept | Out of scope by design — no login, no Firestore for game state, localStorage-only per milestone constraint. No attack surface added. |
</threat_model>

<verification>
- `npm install` reports no ERESOLVE peer conflict for lucide-react/tailwindcss/@tailwindcss/vite against React 19 (Task 1).
- `npm run build` succeeds (not just astro dev) and emits dist/armory/burning-banners.html (Task 3).
- Marketing pages (index/games/tools) show zero new or changed <script>/<link> tags versus the captured baseline (Task 3) — the BB-02 headline check.
- armory-bb-tailwind.css imports only theme + utilities layers (no bundled entrypoint, no preflight) and is imported only from BurningBanners.tsx (Task 1 + Task 2).
- Storage migrated to real localStorage under key bb:campaign with the artifact shim removed; lucide icons remain named imports (Task 2).
- Deeper isolation/SEO/sitemap gates run in plan 10-02; blocking human play-through is plan 10-03 (deferred in the autonomous run).
</verification>

<success_criteria>
- Burning Banners is served at /armory/burning-banners as a client:only React island keeping its own dark look (BB-01).
- Campaign state persists in real localStorage (key bb:campaign) with save + reset preserved (BB-01).
- Tailwind v4 + lucide-react are scoped entirely to the BB route with zero new script/link tags on any Nocturne marketing page (BB-02).
- The build-time parity guard passes and `npm run build` succeeds with no ERESOLVE.
</success_criteria>

<output>
Create `.planning/phases/10-burning-banners/10-01-SUMMARY.md` when done.
</output>